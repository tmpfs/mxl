# vim: set ft=conf:
unlink-window -k -t mxl
new-window -n mxl
send-keys -t: 'vim .' C-m
split-window -h -t:
send-keys -t: 'git status' C-m
split-window -v -t:
send-keys -t: 'npm run cover' C-m
select-pane -L
